<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bewei.dirga_online">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_h"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.bewei.dirga_online.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main activity -->
        <activity
            android:name=".Main2Activity"
            android:label="@string/app_name"
            android:theme="@style/Splashscreen" />
        <activity
            android:name=".ListOther"
            android:label="@string/title_activity_list_other"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".ListMotorCycle"
            android:label="@string/title_activity_list_motor_cycle"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".ListDataCar"
            android:label="@string/title_activity_list_data_car"
            android:theme="@style/AppTheme1" />
    </application>

</manifest>